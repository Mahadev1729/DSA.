#include<bits/stdc++.h>

using namespace std;


int maxSum(vector<int>&nums){
    int res=INT_MIN;
    for(int i=0;i<nums.size();i++){
        int sum=max(nums[i],sum+nums[i])
    }
}

int main(){
    vector<int>arr={};
    return 0;
}
